#include <stdio.h>
#include "EventGenerator.hh"

// using namespace XtcData;

int main() {
  // TypeId tid(TypeId::Parent, 0);
  // printf("hello world: %d\n", tid.version());
  // AnotherClass a(5);
  printf("hello world: %d\n", 5);
}
